enum ButtonType {
  primary,
  secondary,
  success,
  error,
  warning,
  outlined,
  text
}

enum LogoType {
  light,
  dark
}

enum TransactionType {
  all,
  income,
  expense,
}

enum ButtonSize {
  sm,
  md,
  lg
}